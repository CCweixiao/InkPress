# 代码证据契约

## 关系置信度

- `resolved`：语言分析器解析到具体定义或模块。
- `syntactic`：源码中存在明确 import、调用或注解，但目标可能有多个。
- `inferred`：由命名、配置或框架约定推断，必须说明推断依据。

## 引用

每条证据包含项目相对路径、起止行、符号名和不超过必要范围的摘要。禁止只引用目录名。

## 截断

达到节点、边或工具预算时返回 `truncated: true`，说明已覆盖范围和未覆盖部分。不要用部分扫描代表整个项目。
